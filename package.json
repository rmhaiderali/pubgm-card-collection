{
  "type": "module",
  "scripts": {
    "build": "node index.js",
    "preview": "HOST=:: PORT=3000 BASE=/pubgm-card-collection serve-static-files dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "serve-static-files": "^0.1.2",
    "signal-exit": "^4.1.0"
  },
  "prettier": {
    "semi": false
  }
}
