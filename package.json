{
  "type": "module",
  "scripts": {
    "build": "node index.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "serve-static-files": "^0.1.1",
    "signal-exit": "^4.1.0"
  },
  "prettier": {
    "semi": false
  }
}
